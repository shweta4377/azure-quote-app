apiVersion: v1
kind: Service
metadata:
  name: quote-app
  namespace: default
  labels:
    app: quote-app
spec:
  type: LoadBalancer
  externalTrafficPolicy: Cluster
  selector:
    app: quote-app
  ports:
  - port: 80
    targetPort: 8080
    protocol: TCP
    name: http
  sessionAffinity: None
